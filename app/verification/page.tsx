import Link from 'next/link'
import { ShieldCheck, FileCheck2, Users2, ArrowRight } from 'lucide-react'

export default function Verification(){return <main className="mx-auto max-w-5xl px-5 py-12 lg:px-8"><header className="max-w-2xl"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-neutral-500">Verification</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Evidence before opportunity.</h1><p className="mt-3 text-base leading-7 text-neutral-500">SLorigins keeps discovery, provenance and export status visible so a community report is never presented as guaranteed supply or export approval.</p></header><section className="mt-10 grid gap-8 md:grid-cols-3">{[[ShieldCheck,'Community evidence','Photos, local context, contributor identity and origin notes.'],[FileCheck2,'Source review','Supporting references, quality information and legal-export checks where required.'],[Users2,'Responsible sourcing','Only suitable discoveries progress toward a buyer conversation.']].map(([Icon,title,text])=><div key={String(title)} className="border-t border-neutral-200 pt-5"><Icon className="text-neutral-700" size={21}/><h2 className="mt-4 text-base font-semibold">{String(title)}</h2><p className="mt-2 text-sm leading-6 text-neutral-500">{String(text)}</p></div>)}</section><div className="mt-10 flex flex-wrap gap-2"><Link href="/discover" className="inline-flex items-center gap-2 rounded-lg bg-neutral-900 px-4 py-2.5 text-sm font-semibold text-white">Explore discoveries <ArrowRight size={15}/></Link><Link href="/discoveries/new" className="rounded-lg border border-neutral-200 px-4 py-2.5 text-sm font-semibold">Submit evidence</Link></div></main>}
